import 'package:flutter/material.dart';

class AppColors {
  // Primary colors
  static const Color primary = Color(0xFF1A1A2E);
  static const Color accent = Color(0xFFE94560);

  // Background colors
  static const Color background = Color(0xFF0F0F1A);
  static const Color surface = Color(0xFF1E1E2E);
  static const Color card = Color(0xFF252535);

  // Text colors
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0B0);
  static const Color textDark = Color(0xFF1A1A2E);  // Added
  static const Color textGrey = Color(0xFF666666);  // Added

  // Border & status colors
  static const Color border = Color(0xFF2A2A3E);
  static const Color error = Color(0xFFE94560);
  static const Color success = Color(0xFF4CAF50);
  static const Color warning = Color(0xFFFFC107);

  // UI colors
  static const Color white = Color(0xFFFFFFFF);  // Added
  static const Color black = Color(0xFF000000);
}